// Example: Creating a Map with initial values
let initialMap = new Map([
    ['name', 'Alice'],
    ['age', 25],
    ['city', 'Los Angeles']
]);
console.log(initialMap); // Output: Map(3) { 'name' => 'Alice', 'age' => 25, 'city' => 'Los Angeles' }
