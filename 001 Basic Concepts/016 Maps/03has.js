// Example: Checking if a key exists in a Map
let userMap = new Map([
    ['name', 'John'],
    ['city', 'New York']
]);
console.log(userMap.has('name')); // Output: true
console.log(userMap.has('country')); // Output: false
