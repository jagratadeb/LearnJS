// Example: Using Object.keys() to get all keys of an object

const person = {
    name: "Alice",
    age: 30,
    city: "New York"
};

const keys = Object.keys(person);
console.log("Keys:", keys); // Output: ["name", "age", "city"]
