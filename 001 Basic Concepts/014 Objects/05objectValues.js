// Example: Using Object.values() to get all values of an object

const person = {
    name: "Alice",
    age: 30,
    city: "New York"
};

const values = Object.values(person);
console.log("Values:", values); // Output: ["Alice", 30, "New York"]
