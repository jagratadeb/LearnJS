// Example: Using Object.entries() to get all key-value pairs of an object

const person = {
    name: "Alice",
    age: 30,
    city: "New York"
};

const entries = Object.entries(person);
console.log("Entries:", entries); // Output: [["name", "Alice"], ["age", 30], ["city", "New York"]]
